# Hydrus API URL for Vite dev server proxy.
# Requests to known Hydrus API paths (e.g. /get_files, /api_version)
# are forwarded to this URL, avoiding browser CORS issues.
# Default: http://127.0.0.1:45869
VITE_HYDRUS_API_URL=http://127.0.0.1:45869
